# UbU Device Sync and Compartment Contract

**Status:** Canonical design artifact (read-only context)

This is a minimal fixture stub for testing. The real file lives in `ubu-design`.

## 1. Purpose

Defines the Phase 2 sync model and the Device/Compartment vocabulary.

## 4. Core definitions

- `SyncStatement` **(net-new)**: the envelope wrapping a canonical mutation.

---
